# ========================================
# CUI Server 环境变量配置
# ========================================

# ----------------
# 服务器配置
# ----------------
# 服务端口（默认: 3001）
PORT=3001

# 绑定地址（默认: localhost）
# 设置为 0.0.0.0 允许外部访问
HOST=0.0.0.0

# 认证 Token（可选，留空则使用 ~/.cui/config.json 中的 token）
# AUTH_TOKEN=your-custom-token-here

# 跳过 Token 认证（开发/测试环境使用）
# 设置为 true 或 1 禁用认证
SKIP_AUTH_TOKEN=true

# ----------------
# 日志配置
# ----------------
# 日志级别: debug | info | warn | error
LOG_LEVEL=info

# ----------------
# Claude API 配置
# ----------------
# Anthropic API Key（必需）
ANTHROPIC_API_KEY=sk-ant-xxxxx

# API 基础地址（可选，用于代理或自定义端点）
ANTHROPIC_BASE_URL=https://api.anthropic.com

# ----------------
# Google Gemini 配置（语音输入功能）
# ----------------
# Gemini API Key（可选，用于语音转文字）
# 获取: https://aistudio.google.com/apikey
# GOOGLE_API_KEY=xxxxx

# ----------------
# 代理配置（可选）
# ----------------
# HTTPS 代理
# HTTPS_PROXY=http://proxy.example.com:8080
# https_proxy=http://proxy.example.com:8080

# ----------------
# 运行环境
# ----------------
# 环境类型: development | production | test
NODE_ENV=production

# ----------------
# 测试相关（仅测试环境）
# ----------------
# 在测试环境启用认证
# ENABLE_AUTH_IN_TESTS=false

# ----------------
# MCP 服务器配置（高级，自动管理）
# ----------------
# CUI 服务器 URL（MCP 内部使用）
# CUI_SERVER_URL=http://localhost:3001

# CUI 服务器端口（MCP 内部使用）
# CUI_SERVER_PORT=3001
